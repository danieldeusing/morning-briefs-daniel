.DS_Store
__pycache__/
*.pyc
.wrangler/
# editor/backup junk — these previously leaked into public/ and onto the live site
*.bak
*.bak[0-9]
*.orig
# CV PDF is a LOCAL-ONLY runtime input for the jobs routine (contains personal
# contact details) — read from disk by jobs-aggregator, never committed/published.
data/cv/
# .claude/ IS version-controlled (agents, skills, hooks, settings) — only the
# personal/local settings and python caches stay out.
.claude/settings.local.json
