.DS_Store
__pycache__/
*.pyc
.wrangler/
# editor/backup junk — these previously leaked into public/ and onto the live site
*.bak
*.bak[0-9]
*.orig
# The CV is NOT in this repo — it lives in the public repo
# twiced-technology-gmbh/cv-danieldeusing; the jobs routine reads it from that
# repo's local checkout. (Nothing under data/cv/ here.)
# .claude/ IS version-controlled (agents, skills, hooks, settings) — only the
# personal/local settings and python caches stay out.
.claude/settings.local.json
